services:
  - type: web
    name: vaultcall
    runtime: python
    buildCommand: pip install -r server/requirements_server.txt
    startCommand: cd server && uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
    healthCheckPath: /
